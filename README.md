# poestaka-library-system
Poestaka is a web-based library management system built with Next.js and MySQL. This project implements relational database design using ERD and normalization, along with SQL queries to handle CRUD operations, borrowing transactions, and data visualization as part of a database systems course project.
